import SwiftUI

/// 운동 기록 목록 스텁.
/// TODO: 디자인 화면 05 — 필터 칩 · 기록 카드(탭하면 수정 화면) · 빈 상태 · ＋ 추가 버튼.
struct RecordsView: View {
    var body: some View {
        ZStack {
            LoadcastColors.bg.ignoresSafeArea()
            Text("기록 — 구현 예정")
                .foregroundStyle(LoadcastColors.neutral600)
        }
    }
}
