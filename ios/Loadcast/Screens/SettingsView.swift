import SwiftUI

/// 설정 스텁.
/// TODO: 디자인 화면 10~11 — 프로필 · 프로필 수정 · 계정(로그아웃·탈퇴 모달) · 앱 정보.
struct SettingsView: View {
    var body: some View {
        ZStack {
            LoadcastColors.bg.ignoresSafeArea()
            Text("설정 — 구현 예정")
                .foregroundStyle(LoadcastColors.neutral600)
        }
    }
}
